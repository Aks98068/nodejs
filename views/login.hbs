<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - RK Store</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-900 text-white flex items-center justify-center min-h-screen">
    <!-- Navbar -->
    <nav class="fixed top-0 w-full bg-gray-800 p-4 shadow-md flex justify-between items-center">
        <h4 class="text-2xl font-bold text-blue-400">
            <a href="/">CyberSec Portfolio </a>
        </h4>
       
    </nav>

    <!-- Login Form -->
    <div class="w-full max-w-md bg-gray-800 p-8 rounded-lg shadow-lg mt-20">
        <h2 class="text-3xl font-bold text-center text-blue-400 mb-6">Login</h2>
        <form action="/auth/login" method="POST">
            <div class="mb-4">
                <label for="email" class="block text-gray-400">Email:</label>
                <input type="email" name="email" id="email" placeholder="Enter email"
                    class="w-full p-3 bg-gray-700 border border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-400 outline-none">
            </div>
            <div class="mb-4">
                <label for="password" class="block text-gray-400">Password:</label>
                <input type="password" name="password" id="password" placeholder="Enter password"
                    class="w-full p-3 bg-gray-700 border border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-400 outline-none">
            </div>
            <div class="flex justify-between items-center mt-4">
                <button type="submit" class="w-full bg-blue-500 hover:bg-blue-600 py-3 rounded-lg font-semibold">
                    Login
                </button>
            </div>
            <div class="text-center mt-4">
                <a href="/forgot-password" class="text-blue-400 hover:underline">Forgot Password?</a>
            </div>
        </form>

        {{#if message}}
        <p class="text-red-500 text-center mt-4">{{message}}</p>
        {{/if}}

        <p class="text-center text-gray-400 mt-6">
            Don't have an account? <a href="/register" class="text-blue-400 hover:underline">Register</a>
        </p>
    </div>
</body>

</html>
