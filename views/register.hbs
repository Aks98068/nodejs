<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup - CyberSec Portfolio</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-900 text-white flex items-center justify-center min-h-screen">
    <!-- Navbar -->
    <nav class="fixed top-0 w-full bg-gray-800 p-4 shadow-md flex justify-between items-center">
        <h4 class="text-2xl font-bold text-blue-400">
            <a href="/">CyberSec Portfolio</a>
        </h4>
        
    </nav>

    <!-- Signup Form -->
    <div class="flex justify-center items-center w-full mt-20">
        <form action="/auth/register" method="POST" class="bg-gray-800 p-8 rounded-lg shadow-lg w-full max-w-md">
            <h2 class="text-2xl font-bold text-center text-blue-400 mb-6">Create an Account</h2>

            <div class="mb-4">
                <label for="name" class="block text-sm font-semibold text-gray-300">Full Name</label>
                <input type="text" name="name" id="name" placeholder="Enter your name"
                    class="w-full p-3 mt-1 text-gray-900 rounded-lg bg-gray-100 focus:ring-2 focus:ring-blue-400">
            </div>

            <div class="mb-4">
                <label for="email" class="block text-sm font-semibold text-gray-300">Email Address</label>
                <input type="email" name="email" id="email" placeholder="Enter your email"
                    class="w-full p-3 mt-1 text-gray-900 rounded-lg bg-gray-100 focus:ring-2 focus:ring-blue-400">
            </div>

            <div class="mb-4">
                <label for="password" class="block text-sm font-semibold text-gray-300">Password</label>
                <input type="password" name="password" id="password" placeholder="Enter password"
                    class="w-full p-3 mt-1 text-gray-900 rounded-lg bg-gray-100 focus:ring-2 focus:ring-blue-400">
            </div>

            <div class="mb-4">
                <label for="passwordConfirm" class="block text-sm font-semibold text-gray-300">Confirm Password</label>
                <input type="password" name="passwordConfirm" id="passwordConfirm" placeholder="Confirm password"
                    class="w-full p-3 mt-1 text-gray-900 rounded-lg bg-gray-100 focus:ring-2 focus:ring-blue-400">
            </div>

            {{#if message}}
                <p class="text-red-400 text-center text-sm mb-4">{{message}}</p>
            {{/if}}

            <div class="flex flex-col space-y-3">
                <button type="submit" class="w-full bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 rounded-lg">
                    Register
                </button>
                <button type="reset" class="w-full bg-red-500 hover:bg-red-600 text-white font-semibold py-2 rounded-lg">
                    Reset
                </button>
            </div>

            <p class="text-sm text-center text-gray-400 mt-4">
                Already have an account? 
                <a href="/login" class="text-blue-400 hover:underline">Login here</a>
            </p>
        </form>
    </div>
</body>
</html>
